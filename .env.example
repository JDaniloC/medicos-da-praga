# Chave da API do Google Gemini (obtida em https://aistudio.google.com/apikey).
# Copie este arquivo para `.env.local` e preencha o valor. NUNCA comite a chave real.
# Ela é usada apenas no servidor (rotas /api/*), nunca exposta ao navegador.
GEMINI_API_KEY=
